# Spec 14 gap register

> **Source-freeze checkpoint:** This source document records the pre-terminal verification state. The delivered detached handoff report and `handoff/evidence-index.json` carry subsequent build/export, Payments rerun and terminal-journey results with their actual identities. Those later results supersede pending checkpoint statuses here. Keeping results detached avoids changing the source bytes that those executions verify.

**Current moderation status:** the applied incomplete-input migration introduced an active-event schedule regression by omitting 010575's later dynamic rewrite; the published-edits regression caught and preserved that failure. The separately approved supplemental migration is now applied as ledger entry 123 and restores the active-event schedule condition. Independent preservation found the first 122 entries unchanged, 46 of 47 table hashes unchanged with only the existing policy revalidation timestamp moving, all 16 evaluations unchanged, and all 232 function owners/ACLs unchanged; only the intended invalidation function definition changed. All eight moderation SQL suites now pass 494 assertions, and the four-case race retry passed with cleanup verified and every evaluation outside its fixture unchanged. Refreshed type generation and the bound build/restart passed. Connected J06c/J06d processing now passes against the retained valid revision; hosted worker/provider readiness and final assembly remain open.

**Current Gate B working record.** Implemented means code is present; it does not imply a connected journey has passed. Final assembly remains incomplete while B1 and the required runtime matrix are open.

| Gap | Current disposition | Required evidence / remaining gate |
|---|---|---|
| G01 Create inheritance | Approved presentation recovered selectively; newer version-aware writers retained; J01 connected local creation/publish outcome passed with retained event and moderation history. The preview Back-state and published paid-tier query repairs pass the independent 27/27 focused checks and the bound build | B2 passed the same-draft Back/native-history/reconfirm behavior and rendered both retained published paid tiers from one owned-tier read with zero Connect reads. Hosted configuration and final assembly gates remain |
| G02 same-event Stripe return | Existing Payments contract reattached; event and tier IDs plus fresh owner and Connect checks preserved; J01 exercised the same-event local provider-boundary return. The repaired preview state is in the bound build | B2 retains its exact secondary organizer, unpublished draft and two tiers. Four checks pass: same-draft Back/history/reconfirm, exact status failure/retry and return, invalid/wrong-owner exits, and published paid-tier source. One exact external Connect.js drop produced 11 page errors and no inner recovery UI; the frontend-only readiness gate passed independent review. Its rebuilt rerun passed all five behavior assertions but retained one missing simulator setter error, so overall B2 acceptance still requires the corrected-adapter rerun. Additional stale/deep-link coverage remains in J07; hosted Connect remains external |
| G03 free management and admission | Strict free readers, source-aware navigation/metrics/search/ticket selection/recheck are attached to the existing manual and QR authorities | Corrected J03 resume4 passed with the retained original and reverse registrations, both manual/QR directions, stable IDs and Used times. Separate J03a passed the draft-excluded discovery/public-read boundary under the paid-API monitor. Only the exact original paid-event metric on My Events is classified separately; free/unknown paid APIs remain forbidden. Repaired-build responsive and native Cancel-focus evidence passed |
| G04 Auth race | Baseline independently reproduced; public SDK coordination cannot cover SDK initial-subscriber lock escape | Exact auth-js2.112.3 dependency patch proposal UNAPPLIED, pending explicit approval; final Auth readiness blocked |
| G05 artwork | Settled exclusion retained | Null/neutral public media; no upload, storage policy, or live hero work |
| G06 discovery ingress/P01/quota | Neutral price presentation and provisional 60/minute limit retained; independent local trusted-header simulation | Hosted ingress and numeric approval remain separate; no M03 or new migration |
| G07 uncertainty and proof loss | Existing request, bearer, and seen-marker identities retained. A separate J08 browser run passed one-shot loss and explicit real retry for the original free email-status read, cached selected-free detail refresh, and free public-ticketing read with zero writers and exact ticket/scenario/provider preservation. J08g retained its local failure: natural minimum retry returned `410 CHECKOUT_EXPIRED`, then the same completed provider Session's signed payment event was permanently rejected as `PAYMENT_SNAPSHOT_MISMATCH`; the original order remains `creating_checkout`, unattached and ticketless, with Confirming UI | The J08g contract remains paused with its unapplied proposal; no replacement or SQL repair was used. RSVP/admission boundaries and corrupt/missing/lost-proof cases remain separately tracked; complete proof loss remains unrecoverable. Separate UI-created sources now prove page-two failure at 25 admissions and recovery to 30, plus a capacity-one public full state, using 31 real tickets from four real registrations. Corrected-origin reruns record zero writers; earlier zero-writer counters are qualified because they watched port 55647 while browser requests used same-origin port 3040 |
| G08 providers | Local real services plus provider-boundary simulators present | Hosted target, configuration, and transaction proof remain unapproved; local evidence is not deployment proof |
| G09 legal/support | Existing explicit available/unavailable capabilities retained; development policy pair local-only | Exact hosted recipients, limits, policy treatment remain separately gated |
| G10 map | Settled exclusion retained | No map increment; current address search continues to use a local provider simulation |
| G11 safe return | Exact organizer route grammar covers every registered leaf with typed event/order/registration/ticket UUIDs; untrusted query, fragment, relative, external and malformed destinations are excluded | Focused route tests pass. B1 still blocks final Auth coordination; remaining browser deep-link and expiry cases stay in J07/J08 |
| G12 fixture gallery | Explicit `WHERETOO_ENABLE_PREVIEW=1` opt-in implemented; Vercel Preview alone leaves it off | Red-to-green source tests and a bound-build production run passed 10 checks across all 149 served artifact files; a later final frozen export/build is still required |
| G13 portable harness | New task-identity runner, the exact 121-migration inherited baseline plus two approved targeted migrations as ledger entries 122 and 123, explicit local types, real Auth/inboxes, guarded control CLI, and four closed launcher roles are present | Both targeted migrations applied with the inherited 121-entry prefix and the complete first 122 entries preserved across the supplemental interval. The four race cases, refreshed type generation and bound build/restart pass; interactive walkthrough and final standalone export remain pending; earlier harness and four-role read-only smoke evidence remains valid within its recorded source/build bindings |
| G14 transaction ingress | Local gateway overwrites input headers; production handler trust model retained | Direct hosted gateway, network address translation, and spoofing proof require later approval, as does any resulting bounded contract change |

### Named subgap: incomplete draft moderation input

The retained B2 tier save queued a contextual evaluation before required disclosures existed. The global worker later claimed that earlier job and returned 500: the exact offline schema proof found seven null disclosure fields and no other invalid field. That original failure is preserved.

The approved `20260918010100_guard_incomplete_moderation_jobs.sql` migration (SHA256 `992a0de9840f90cf79d887215ab2d966b7857099698842fb4a17cea35c345f6e`) has now applied as migration 122. All 121 inherited migration identities remain exact. The migration retired the retained incomplete job as `INCOMPLETE_MODERATION_INPUT` while preserving its ID, input hash, revision, attempt 1 and start time; all 15 other moderation jobs were unchanged. Whole-state preservation found 45 of 47 table hashes unchanged, with changes limited to the permitted evaluation terminal fields and the existing policy-development revalidation timestamp. All 230 existing function ACLs remained unchanged; one invalidation condition changed and two functions were added. Enqueue checks pass 4/4 and the new SQL contract passes 32/32 after two preserved pgTAP harness failures.

A broader SQL run then exposed and preserved the 122nd migration's active-event schedule regression. The separately approved `20260918010200_restore_active_event_moderation_schedule.sql` migration (SHA256 `827b264600d5e07adf0d2e8227ea0a284b56ed1f4c7bad09caf8af4aae89a087`) is now applied as entry 123. Independent before/after comparison found 46 of 47 table hashes unchanged, with only the existing policy revalidation `updated_at` moving; all 16 evaluations remained identical. All 232 function owners/ACLs remained identical, and only `private.invalidate_event_public_revision` changed definition from hash `b32f90c4…` to `e20ff4f…`, restoring the inherited `ends_at > now()` schedule condition while retaining the incomplete-input guard. The first 122 ledger entries remain exact and the complete 123-file migration inventory matches the approved union. All eight moderation SQL suites pass 494 assertions (`73+4+32+152+49+61+36+87`), bound by `.superpowers/spec14/moderation-supplement-sql-evidence.json`. The four-case race retry passed with cleanup verified and every evaluation outside its fixture unchanged.

The targeted moderation repair is now closed locally: the four-race retry passed with cleanup and every outside evaluation unchanged; refreshed type generation and the bound build/restart passed; connected J06c/J06d processed the retained valid revision and preserved the intended postponement/stale-notice behavior. Hosted worker activation remains outside this evidence. The original incomplete job and failed worker attempt remain preserved, with no replacement application response.

## New demonstrated dependency decision

The B1 Auth dependency patch requires separate implementation approval. G07 also retains its separately unapproved payment reconciliation contract proposal. Installed `@supabase/auth-js@2.112.3` starts initial-session work inside the public custom-lock callback without awaiting it. A refresh can outlive that lock; the internal Supabase client subscriber exposes no public readiness/error result. Independent diagnostics and the exact unapplied patch are in task evidence under `.superpowers/spec14/auth-dependency-proposal/`.

The proposed dependency patch preserves global signout and the existing subscription return, keeps initial work under the lock, and exposes a nonrejecting readiness/error promise. Acceptance additionally requires application readiness/error handling, signout completion ordering, truthful global-signout copy, actual shared-origin multi-tab controls, and independent review. No patch/upgrade is implied by Gate B's earlier public-API approval.

## Retained limitations

- Simulated Stripe/email/address/moderation does not establish hosted acceptance, received email or physical-camera proof.
- No recovery promise after total loss of private payment proof; no replacement transaction is allowed to hide an unknown outcome.
- Historical SQL checks remain at 62/64. Both metadata expectation failures are recorded separately and their assertions remain intact. The current-contract replacement passes its separate five-test serial run; that result does not alter the historical record.
- The latest frontend run passes all1,544 assertions across181 files; full typecheck and lint exit0. Its test command still exits 1 because 15 unhandled Auth coordination errors remain visible while G04 is unresolved.
- Ten selected Python race scripts pass. The original admission shell race script passes only through the recorded timeout/deadline transport adaptation; original and adapted hashes are retained.
- The organizer, paid-buyer, free-buyer, and staff launcher roles pass the headed read-only smoke. J01, J02, and corrected J03 connected cores pass locally on retained originals. J04's principal initial/recovery delivery and J04a's failed/unknown resend paths pass locally; later delivery-purpose cases and J05 remain separately tracked. J06's original temporary, title-scoped legacy-history proof passed with truthful saved/public current facts, explicit missing previous-state values, one normal history snapshot, exact cleanup and preserved scenario/provider state. The first corrected-origin rerun stopped when its fence identified two legitimate, self-scoped `get_my_staff_role` reads; cleanup passed and sequence 162→163 was not rewound. After that reader was added to the closed allowlist, the same fixed-ID control passed with truthful current saved/public facts, explicit null previous facts, one ordinary snapshot, zero writers/blocks/page errors, exact cleanup and sequence 163→164 without rewind. Corrected J08 read-recovery passes three real read-loss/retry cases, and strict read-only real-data evidence passes 25→30 pagination recovery plus a separate capacity-one full state with exact preservation. B2 passes four same-event/authority checks and retains one external-script recovery failure. The remaining J07–J08 dispositions and interactive walkthrough stay open.
- Two targeted moderation-contract migrations are approved and applied as described above; no other new domain migration is approved or added. Hosted jobs/providers remain inactive/unconfigured by this task.

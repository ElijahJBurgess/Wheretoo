# Spec 14 — final assembly verification

> **Source-freeze checkpoint:** This source document records the pre-terminal verification state. The delivered detached handoff report and `handoff/evidence-index.json` carry subsequent build/export, Payments rerun and terminal-journey results with their actual identities. Those later results supersede pending checkpoint statuses here. Keeping results detached avoids changing the source bytes that those executions verify.

## Current status

**Gate B is in progress. Final local assembly is not yet verified.** Both approved moderation migrations are applied; all eight targeted SQL suites (494 assertions), four concurrency cases, independent preservation checks, and the connected J06c/J06d journeys pass. The current main browser matrix is 21 passed, one failed and paused (G07/J08g), and ten unexecuted. The separate Auth dependency proposal remains unapproved; the latest full frontend command has1,544 passing assertions across181 files but exits1 with15 Auth coordination errors. The remaining browser branches, final source freeze, and clean export are open. Nothing in this report claims deployment, hosted configuration, real email delivery, or a real Stripe transaction.

The approved destination is `.worktrees/wheretoo-v1-final` on `codex/wheretoo-v1-final`. Its starting HEAD is `94c546bd0961f501584f8a3437298b2331cafd5c`. HEAD alone does not represent its inherited source. Before editing, the resolved Spec 13 chain was materialized and verified as 1,017 entries: 1,013 present files and 4 intentional deletions. The final count will be measured after all changes; it is not constrained to that baseline.

All source worktrees, main, their existing Git staging/index contents and unrelated map work remain protected. The independent preservation rehash passed for all 29 protected trees and 17,200 tracked/nonignored records: bytes, modes, symlinks, deletions, HEAD/branch, status/staging, and semantic index hashes match the initial inventory. This is recorded in `protected-tree-inventory.final-independent.json`. No Git staging, commit, merge, push, deployment or hosted/provider operation has been performed. Two separately approved moderation forward migrations have been applied only to this task’s disposable local database.

## Fresh verification evidence

Counts below describe individual suites. They are not summed as unique coverage; many overlap. Raw private evidence lives in `.superpowers/spec14/` and review reports in `.superpowers/sdd/Spec14-Gate-A-Plan/`.

| Check | Fresh result | Evidence / limitation |
|---|---|---|
| Exact baseline materialization | PASS | `materialization.json`, `inherited-source-manifest.json`; bytes/modes/symlinks/deletions checked |
| Baseline offline frozen dependency install | PASS | No sibling dependency directories copied |
| Baseline frontend suite | 169 files / 1,406 tests PASS | `baseline-unit.log`; before Spec 14 edits |
| Migration replay | 121 unchanged historical migrations plus2 approved forward migrations PASS | Exact append manifest, task identity and cron-off guards; all previous122 ledger entries unchanged after the supplemental append |
| Generated types | PASS | Explicit local postgres-meta service/public schema; `typegen-identity.json`; never linked target |
| Function tests | 347 PASS, exit0 after moderation repair | `moderation-forward-all-functions.log`; previous342-test checkpoint retained |
| Function typecheck | PASS | `functions-typecheck-final-candidate.log` |
| Production build | PASS at current checkpoint | Gallery disabled, CSP retained; post-moderation source `91897690…`, assets `738da489…`, 149 files. Guarded build/start/verify-running exited0; data preserved. Read-only HTTP rehash matched all149 served assets (`moderation-supplement-served-asset-proof.json`). Later source changes require final rebinding |
| Explicit gallery opt-in | 2 tests PASS | Red→green; Vercel Preview alone does not enable fixtures |
| Selected inherited SQL | 62/64 suites PASS, 2 FAIL | Original assertions retained; see failures below |
| Current metadata contract replacement | 5 tests PASS in the final serial run | `sql-assembly-serial.log`; this current-contract result does not erase the two failures in the original inherited suite |
| Python race suites | 10/10 scripts PASS | `python-regressions.json`; each entry records the original source hash and exit code |
| Admission shell race/collision suite | PASS with transport-only adaptation | Original script and recursive fixture hashes retained; only timeout/deadline transport values were adapted, and the adapted hash is recorded in `shell-regressions.json` |
| Local runner/artifact/transport/adapters | 71 Python tests PASS, exit0 | `local-tooling-with-forward-inventory-final.log`; includes append-manifest guard tests. Mock export output is not actual export evidence |
| Manual/control/browser helper tooling | Earlier26 Node tests PASS; corrected browser guards29/29 independently PASS | `browser-guard-independent-review.md` covers three same-origin writer fences, B2 action counters, imported-source binding and explicit retained-data read-only mode. Actual reruns remain separate |
| Create reconciliation | Scoped suites and connected J01 core PASS | Selective recovery, same-event Payments, original-CSP address lookup, real held-event review and anonymous discovery; see journey evidence below |
| Preview review closure | 27/27 tests independently PASS, exit0 | Both Stripe-prompt lifetime and published paid-tier findings closed at exact two-file hashes; bound rebuild passed. Corrected B2 browser run passes prompt Back/history/reconfirmation and published two-tier preview; its separate external-script error stage fails |
| Free attachment | 57 scoped tests PASS | Agent report; independent review closed |
| Paid manual-dialog lifetime correction | 6 tests PASS independently rerun | Same-owner identity generation change disposes old pending dialog |
| Root frame/preview integration | Focused tests and J01 core PASS | Creation frames and only 3 design-preview captures regenerated; real application composition/publication verified separately in J01 |
| Actual local Auth smoke | PASS | UI signup → captured Auth confirmation → callback → profile → Payments; separate pre-reset controls |
| Auth race | Reproduced, unresolved | Public SDK initial subscriber escapes lock; exact patch awaiting approval |
| Latest configured full frontend checkpoint | 181 files /1,544 assertions PASS; process exit1 | `b2-readiness-full-frontend-execution.json`;15 known unhandled Auth coordination errors remain visible. Earlier1,527/1,532 checkpoints and configuration failures are retained. No claim of a green full command |
| Current full typecheck/lint | PASS, both exit0 | `b2-readiness-full-typecheck-execution.json` covers all four projects; `b2-readiness-full-lint-execution.json` covers full ESLint after the Payments readiness gate |
| Static production boundary scripts | PASS, exit 0 | Spec 07 scanned 139 current `dist` artifacts; ticket-shell source/bundle boundary passed. These are read-only checks against an interim build |
| Principal J01–J08 | Partial | J01/J02/J03/J04 cores, J02a/J03a and J07a PASS; later delivery/refund/event-change/Settings/failure branches pending at this checkpoint. Auth remains blocked. See exact journey table |
| Artifact identity tooling | Independent closure PASS | Exact pinned pnpm/runtime/store isolation, latest-attempt fencing and final source/dist rechecks independently reviewed; overlapping artifact+runner37 and aggregate Python60 passed. Actual export still pending |
| Clean export/rebuild | Pending | Final source catalog, offline install, standalone rebuild, and identical `dist` comparison wait for source freeze |
| Manual launcher | Four headed smokes and four ordinary launch lifecycles PASS | `manual-launch-lifecycle-summary.json`; each exact CLI reached ready, owned main browser closed, launcher exited0, active record removed, profile retained, provider state unchanged. Earlier transient and harness-filter failures remain recorded. This proves launching, not completion of the walkthrough |
| Responsive/keyboard/200%/motion | Partial, inspected runtime evidence | 90 populated captures opened across all five widths/two text sizes; all inspected defect categories repaired; latest MyEvents10 reflow pass closes narrow mobile free-card copy. Separate Settings26 inspected PASS, motion2 inspected PASS, native door3 PASS with zero admission writes; see visual section |

### Retained SQL failures and replacements

1. `moderation_policy_acceptance.test.sql`, assertion27, broadly aggregates `accept_current_event_policies_%` signatures. The later guarded function adds `p_expected_context`; the older assertion expects only `p_event_id`. The original failed result remains. The separate current-contract suite checks exact base/guarded signatures and ACLs.
2. `spec08_checkout_expiry.test.sql`, assertion2, expects cron job ownership by the test session's `supabase_admin`. Replay records the installing migration role as `postgres`; job is inactive and browser execute privileges remain denied. The separate current-contract suite checks the exact installer role, definition, inactive state and ACLs.

These are metadata expectation mismatches. They do not demonstrate a new domain-schema defect. No historical test assertion or migration was changed to obtain a passing count.

## Connected browser evidence

All results below are local in this chat. Provider-boundary simulation is explicitly distinct from real Stripe TEST or email delivery. A test authored/collected/typechecked is not an executed journey. The private scenario ledger preserves original identities, proofs and every failed/interrupted attempt; no original history was reset during the principal phase.

| Journey | Executed evidence at this checkpoint | Remaining proof |
|---|---|---|
| J01 | Real signup → separate Auth inbox → confirmation → profile/deferred Connect → paid draft/two tiers → current agreement → persisted preview → same-event Connect return → explicit publish → held moderation → organizer review request → distinct staff Apply Clear UI → anonymous discovery of original event. `j01-browser-resume5.log` / ledger J01 | Supplemental negative/return variants mapped to focused tests and database controls; broad Auth race remains B1 |
| J02 | Same paid event; 2 GA + 1 VIP; one order/two items/three tickets; real signed local webhook; all three original QR captures; manual ticket2 → QR duplicate preserves original Used time/IDs/hash and siblings. `j02-browser-resume1.log` | J02a paid QR-first → manual duplicate also PASS (`j02a-browser.log`); lost-response controls remain separate |
| J03 | Original UI free event/quantity3 registration and explicit separate reverse registration retained. Manual2 → QR duplicate and QR2 → manual duplicate; siblings and Used times preserved. Corrected `j03-browser-resume4.log` PASS. `j03a-browser.log` separately passes full free draft authoring with zero forbidden paid APIs and absence from discovery/direct public | Only the exact original paid card's My Events metric read is classified separately; free/unknown paid APIs remain forbidden. First principal creation's incomplete monitor is not retroactive proof |
| J04 | `j04-browser-resume1.log` PASS: one permanent initial receipt per original paid/free source; queued/accepted state; mounts/reloads send nothing; exact provider messages and fresh links show original tickets; real neutral recovery/outbox/provider binding. J04a PASS for original failed/unknown resend continuity. J04b PASS (`j04b-browser-resume1.log`): two actual three-ticket collections with exact recovery delivery binding; wrong membership, missing token and other private purposes denied | Expired/revoked and refunded/cancelled-source cases remain pending |
| J05 | Strengthened executable principal refund ledger/snapshot/inventory/history/financial-link scaffold, independently reviewed | Execute original order plus separately named failure/unknown/anomaly sources |
| J06 | J06b stale context, repaired J06c same-revision postponement/recovery, and J06d superseded/accepted notice cases PASS. Legacy-history UI and fixture preservation PASS, with its old writer counter qualified below | Main cancellation and late-payment branches remain pending. Original moderation and recovery failures are retained; no replacement revision/request closed them |
| J07 | J07a unsaved Keep editing, real same-owner competing profile write, stale rejection/review, explicit resolution/save/reload PASS (`j07a-browser-resume1.log`); account identity fields unchanged | Account/email/password/reauth routes pending; shared-origin Auth race cannot pass without B1 approval and repair |
| J08 | Nine main failure-recovery cases PASS; G07/J08g failed and paused. Separate read-recovery UI outcomes and real30-row pagination/capacity1-full outcomes PASS with exact selected-data preservation | The corrected read-recovery, retained-data and legacy-control reruns now pass with both-origin fences. Old zero counters remain qualified. The31 real tickets and all original source identities remain retained |

Only the distinct confirmed synthetic staff account's existing `private.staff_roles` prerequisite was inserted under the explicitly approved task-only role bootstrap. The staff UI performed the actual review. No principal public state or ticket/order success row was injected.

### Retained browser failures and source repairs

- J01 retained account/draft through interrupted and locator-failure attempts. Original CSP exposed the installed Mapbox web component's dynamic inline-style failure. The repair uses the installed public `useSearchBoxCore` / `useSearchSession` APIs and ordinary imported CSS. No package/lock/CSP/origin change. Original same-event draft then completed under the original CSP. Nine failing location tests preceded the 11-test green suite; a combined 49-test check also passed.
- J02's first duplicate locator expected “Already used”; the actual scanner says “Already scanned.” Only the lookup changed; the original order/tickets and admitted timestamp were retained.
- J03 resumed original request/bearer sources after navigation timing failures. It never used “Start another RSVP” to clear uncertainty. The legitimate paid-card reader diagnosis is retained, followed by the passing corrected monitor run and separate J03a draft proof.
- J04 first stopped before the worker because the test expected “Used” while the wallet says “Already used”; only the test lookup changed. J04a then queried SQL before the browser resend acknowledgement had settled; its original request and failure were retained, and the corrected same-request retry passed. Neither is a replacement transaction.
- J04b resumed the existing recovery event after one bounded real moderation-worker invocation processed its queued revision3. No event/revision replacement or forced clear occurred. The intended separate RSVP was created through the real UI; recovery then returned two actual collections and passed membership/purpose isolation. Queue and browser evidence are recorded separately.
- `j04b-same-grant-proof-resume1.json` adds a read-only comparison of both collections' exact canonical ticket-ID/status arrays, three each, using the original retained grant. Zero writer attempts; source/ticket facts preserved; no new recovery/send/RSVP. An earlier standalone assertion failure remains retained with insufficient diagnostics to establish its cause. The resumed check used the ordinary40-second expectation timeout and passed; this does not erase or diagnose the first failure.
- The native free admission dialog originally removed its opener. The repair keeps the opener mounted while the native dialog owns modality, matching the paid implementation. A focused red→green test and 13 scoped tests passed. Escape now returns focus; camera controls and tests perform zero admission writes.
- J06c's first run saved and published the intended date change as revision5, then failed a test helper that waited for the old `publish_event` endpoint and a second confirmation. Current source uses `publish_event_if_current` with a context envelope, and published-event **Publish changes** dispatches directly. The helper was corrected; recovery continues from the exact existing revision5 and its queued moderation evaluation, without publishing another date/revision. This retained failure is a diagnosed test mismatch, not a claimed successful full J06c run.
- J08 core passed on the new bound build (`j08-browser.log`): malformed links do not initialize checkout, anonymous organizer access is guarded, and the original free collection preserves its tickets across offline/reconnect. J08a’s first run retained a public selection URL because its harness captured navigation too early, then chose a recovery action before it rendered. A writer-fenced read of the correct same-cart checkout route showed the original attached Session/order and eventual Check status control. The test now waits for the actual checkout route and rendered recovery action. `j08a-browser-resume1.log` passed corruption guards, same-request/Session recovery and signed fulfillment of three tickets on that same order; the wrong URL and failed attempt remain recorded. This does not close the distinct unattached-session G07 failure.
- Account Actions copy now describes requesting sign-out of other organizer sessions. Seven focused tests pass; global sign-out semantics were preserved. This independent copy correction does not close B1.

### J06c — confirmed incomplete-draft moderation job

The second retained attempt, `j06c-browser-resume1.log`, reached the actual moderation worker and returned500. This is separate from the first publish-acknowledgement test mismatch. The global FIFO worker claimed the earlier retained B2 paid draft, whose tier-text save had queued a contextual evaluation before a disclosure row existed. It did not reach the postponed event’s revision5 evaluation or make a provider request for the malformed job.

Guarded read-only capture found one processing B2 job, attempt1, with all seven disclosure fields null. An offline cached Deno invocation imported the actual `moderationJobSchema` and rejected exactly those seven fields: `j06c-exact-job-schema-proof.json`, SHA-256 `0878869515da7887bfefc141da14c386422d7f64a9d52fc0a8cc535971b97b15`. Claim commits before strict parsing returns; a parse exception prevents the handler from retaining the claimed identity, so its generic500 path cannot record a durable failure. The job remains leased until existing queue rules act.

The user approved the independently reviewed targeted proposal (SHA-256 `4c49df7ee5fa5126933279a5c45ced68626235ee72e97f5d40e5b9b6272ef172`) for local repair and verification. Independent implementation review is complete. Both approved forward migrations are applied. The worker was rebuilt and restarted; the original J06c revision5 evaluation completed through the real worker, and the same-request recovery readback and J06d connected notice checks passed. No global worker retry, direct queue update, forced clear or replacement publication was used to hide the failure. Normal completion of the same B2 draft’s required UI fields may legitimately create a later complete revision and supersede its old evaluation; that fixture progression must be recorded separately and cannot close this contract defect. The original postponed event/date/history remain preserved.

### Preview findings — independent source closure

Independent review found a Stripe-required prompt that survived Back/route changes and a published paid preview whose owned-tier query was disabled. The bounded repair scopes the prompt to the route lifetime and explicit confirmation, uses the existing Back callback, and enables the existing tier reader for all paid previews while preserving draft-only Connect checks and zero free paid-reader calls. No copied Create view or backend authority changed.

The independent reviewer ran27 focused tests, exit0, and verified that only the two authorized preview files differed in its799-file production snapshot. Root inspected the exact closure diff and hashes: `EventPreviewPage.tsx` `f5459013f04f64d0a9011c85432c25387ec385724d87b327865632be02c7c2d4`; test `1d84b99f21452d09f936d47646ea45fe846e0220b1e03098df796cecbbaca401`. The author’s combined36 tests passed; build25 had375 passing assertions but exit1 from the known Auth error and remains a failed command. The new bound build and guarded restart passed; actual preview browser proof remains separate.

### J06 legacy-history negative control

`j06-legacy-history-1789445154263/report.json` passed on the current `2d4edf38…/e6246e51…/f7195519…` application artifact. A separately reviewed, task-guarded temporary fixed-ID event copied the original eligible event's facts and approval dependencies without its history. The actual history reader created one current snapshot/state. The real page showed both unavailable-previous-version messages and correct current saved/public venue and address. Current saved/public pointers agreed, both previous pointers were null, and event eligibility/source facts were preserved. The report recorded zero browser writer/outbound/pageerror counts; its writer predicate missed same-origin3040 requests, so that counter is not fence proof. The recorded original scenario/provider preservation and UI outcomes remain valid for their narrower scope.

Setup and cleanup used transaction-local trigger suppression only for that separate fixture, restored before the browser. They added no domain migration or persistent policy change. The task-verified residue report shows zero remaining fixture events, states, snapshots, intervals, actions and acceptances. The real reader consumed one ordinary sequence value,22→23; cleanup deliberately did not rewind it.

Earlier attempts remain recorded: an invented interval/action-equality guard first stopped before setup; source inspection removed only that guard, since the actual eligibility predicate validates those facts separately. Two subsequent browser attempts reached the unavailable-history states but failed an overly broad title locator matching collapsed detail nodes. The final run narrowed title/current-fact locators to their actual sections. It changed no application source or semantic expectation. This negative control does not replace the pending connected event-change/cancellation journey.

### G07 / J08g — confirmed unattached paid checkout gap

`j08g-browser-resume1.log` exits1 with a demonstrated application/contract failure. A separate UI-created negative order retains its one original provider Session after a simulated create response is lost. The current database uses the later minute-snapped39-minute expiry; the handler needs34 minutes remaining for its create/retry envelope. After ordinary time crosses that minimum, the same original UI retry returns410 `CHECKOUT_EXPIRED`. The same simulated provider Session subsequently becomes complete/paid and enters the authentic signed local webhook. The receipt records `PAYMENT_SNAPSHOT_MISMATCH`; the order remains `creating_checkout` with a null attached session and zero tickets. Its original confirmation UI remains “Confirming your payment.”

The actual task database definitions are captured in `j08g-current-function-definitions.json`: the current private snapshot lookup still requires the order's attached session ID to equal the incoming session. The public wrapper delegates to it. No alternate payment identity, handler/SQL repair, assertion relaxation or domain migration was applied. The bounded atomic recovery and guarded-review proposal was submitted for separate approval and remains unapplied; this branch is paused while independent verification continues. Normal redelivery skips the already-processed failed receipt, so its historical remediation is a separate decision. The old failure is retained. This is a local simulated-provider failure, not a real Stripe charge.

The earlier `j08g-browser.log` failure stopped before retry/webhook because the test derived its wait from the older35-minute definition and imposed too small a bound. The later39-minute override was verified in the actual database, the same existing order/Session was retained while other work ran, and the resumed case then demonstrated the real contract gap. The timing mistake remains separate from the confirmed payment-evidence failure.

## Visual and production evidence

Every cited accepted screenshot was opened; geometry alone was not treated as visual proof. Private screenshots may contain synthetic identities or bearer routes and are excluded from export/commit.

- `visual-journeys-1789438941110`: first populated 90-capture sweep, exit1, 16 document-overflow cases. Root opened discovery/events/paid-public30; independent reviewer opened remaining60. Visible findings included public hero expansion, wallet/status clipping, free-search status clipping, enlarged check-in navigation covering actions, paid My Events row overlap, and discovery glyph clipping.
- `visual-journeys-1789440007535`: repaired source `116aa879…`, assets `4c970857…`, index `c3d30fce…`; all90 opened. Zero document overflow; all check-in end-of-document actions clear. Ten measurement flags came only from hidden zero-size paid arrows; a targeted rerun (`1789440158433`) reports zero layout violations after the measurement was corrected. Root image review still found narrow free-card copy at320/text2: recorded as an open finding, with a new guard red in six mobile cases (`1789440596853`). The subsequent `visual-journeys-1789441177052` run is bound to `123aeedf…/619d34cc…/37c55c5a…`:10captures, zero layout violations; all10 opened by root, and mobile free-card copy now uses the full row. The new guard proved red before the source repair.
- `visual-settings-1789436645576`:26 Settings captures independently opened; sidebar brand overflow at768/text2 repaired from55.484px to0. Final-candidate recheck `visual-settings-1789441307214`:26 captures, all opened by root, passed including truthful global-signout copy and unavailable optional support/legal links. This recheck binds `123aeedf…/619d34cc…/37c55c5a…`; later discovery-only CSS does not alter Settings.
- `visual-door-accessibility-1789440017012`: three native camera/keyboard cases PASS, images independently opened. Camera denial/manual fallback, blank synthetic stream/track shutdown, keyboard search/selection/confirmation/cancel, Escape and focus return. Native Tab may traverse browser chrome; background app focus is forbidden. Both admission writer routes fenced, writer count0 and exact ticket facts unchanged. Physical camera/device behavior is unverified.
- `discovery-word-wrap-production.log`:latest10 checks PASS, all149 served files byte-identical, seven deep-link/reload cases, missing asset404, lazy startup-chunk error/recovery, no unexpected outbound/pageerror. `VERCEL_ENV=preview` alone does not enable the gallery. Bound to `2d4edf38…/e6246e51…/f7195519…`; retained result `production-browser-result-discovery-wrap-retained.json`. Earlier independent pass is kept separately; final source freeze still requires rebinding.
- `visual-motion-1789442349049`:latest two390px loading cases opened by root; actual held discovery read; four active skeleton animations normally, zero with reduced motion; real ready state after release. Binds `2d4edf38…/e6246e51…/f7195519…`. Earlier independent two-image motion pass remains separate. Still pixels do not themselves prove motion.
- Discovery follow-up found a regression introduced by global heading padding: at390 normal, “Somewhere” split its last letter. Targeted guard failed1/10 in `visual-journeys-1789442176597`; only its390normal image is cited as opened proof. Padding now applies only at≤359px, retaining the320/text2 glyph fix. `visual-journeys-1789442318087`:10captures, all opened by root, zero violations, normal heading stays whole. The responsive change is narrowly scoped; no type shrinking or overflow hiding.

## Artifact tooling caveats

The source/build tool now records regular-file bytes and modes, symlink targets, and intended deletions. It refuses a parent repository as the export's Git root, uses a committable path catalog for standalone inventory, rejects unlisted files, isolates package-manager configuration from developer home directories, and binds the source identity before and after each build. A clean export must perform an offline frozen-lockfile install from the explicitly selected local store, run source/build capture inside the export, and compare the complete `dist` record with the task build.

Those controls have pure filesystem coverage, but the final path catalog has deliberately not been generated. It must be generated after these documents and all other source changes stop. The actual clean export and identical-build comparison remain pending, so no current source or asset hash is a final handoff identity.

### Read-recovery diagnostic limitation

The first J08 read-recovery run passed email-status recovery, then stopped because `getByRole('alert')` matched two nodes. A separate read-only diagnostic proved the exact detail request was dropped once and the intended refresh warning existed. The exact-text locator correction passed all three cases against the unchanged application artifact. Verification script hashes are recorded independently before/after execution; these test-only edits await final source rebinding.

### Retained lifecycle limitations

After the discovery rebuild, guarded stop succeeded. The first immediate start exited1 before a process registry existed. A later read-only listener check found none, and an exact start retry passed, followed by complete running-identity verification. The first detailed error was overwritten by the next verify command; its cause is unproven and is not labeled a product or port-reuse defect. Logs `discovery-word-wrap-start.log` and `discovery-word-wrap-start-resume1.log` preserve both outcomes. No reset occurred.

The current runner now retains each later error in its own timestamped, action-specific private log. Independent runner14 and aggregate Python60 checks passed, including preservation of the original start error after a subsequent verification error. This repairs evidence retention; it does not establish the old failure's cause. No speculative bind retry was added. The manual walkthrough documents inspection of the error, process registry and listeners before any exact start retry.

## Runtime/service evidence level

- Real application routes and production dist; no `/preview` substitute.
- Real local GoTrue Auth and separate Auth SMTP inbox.
- Real PostgreSQL/PostgREST, authorization/RLS, RPCs, 22 production handlers, fulfillment and reconciliation.
- Stripe, transactional email, contextual moderation and address lookup replaced only at their provider boundaries. Signed Stripe fixtures enter the real webhook.
- Generated QR image fed through a synthetic camera stream tests the decoder/admission chain; it is not physical-device evidence.
- Principal organizer/events/sources must be created through UI. Negative/race prerequisites and deliberate terminal datasets are documented separately; no principal public/ticket rows may be injected.

## Review status

Auth was reviewed independently by the Create reviewer; the free attachment by the Auth reviewer; root's local harness by the provider reviewer. The provider reviewer's own gateway fixes are labeled self-review. An independent reconciliation review of an earlier source checkpoint found no authority downgrade, migration/dependency/map drift, or wholesale Create copy; its Auth blocker remains open, and it predates the newly demonstrated G07 failure. Ten approved direct copies match exact hashes. The later five-file responsive CSS delta also received independent source review with no actionable finding (`review-responsive-app-delta.md`); that review cites existing screenshot evidence without claiming another browser run. Root's current free/Create/Payments follow-up review is explicitly not independent or whole-branch acceptance. Final source/documentation and the evidence index still require a frozen review. Review closure never substitutes for runtime proof.

A fresh reviewer who authored none of the production changes independently checked the full production reconciliation (`review-production-reconciliation-independent.md`). All1,017 inherited entries,121 migrations,91 function files/22 entrypoints, dependency/CSP boundaries and10 direct copies matched; its799-file before/after binding had zero drift. It found two new preview defects: the Stripe-required prompt cannot dismiss through Back and can survive an event switch; published paid Ticket Selection receives an empty array because its owner-tier reader is disabled. Both bounded frontend repairs and27 focused regression tests passed. The later B2 browser supplement remains separately reported. The earlier Create-author reconciliation pass is independent for other agents' changes, but not for that author's own Create implementation; the fresh review closes that reviewer-independence gap without claiming the two findings fixed.

## Handoff documents

- [Flow/route matrix](spec14-flow-route-matrix.md)
- [Manual walkthrough](spec14-manual-walkthrough.md)
- [Staging readiness](spec14-staging-readiness.md)
- [Gap register](spec14-gap-register.md)
- [Commit/deployment proposal](spec14-commit-deploy-proposal.md)

The final handoff will bind all accepted evidence to the final source, dependency, migration and served-build identities. Until then, this is a working record, not a completion certificate.

### Approved moderation repair — pre-application checkpoint

The unchanged-schema SQL regression reproduced the defect:2 assertions passed and2 failed, with one unwanted queued evaluation and a nonnull immutable action reference; its transaction rolled back. The exact RED evidence is retained separately. The append-only runner extension passed71 aggregate Python tests; independent review reran11 inventory tests and four path-rejection probes. All121 historical migration hashes match. After the bounded worker change, the complete offline function suite passed347 tests, exit0 (`moderation-forward-all-functions.log`). These checks do not yet establish migration, concurrency or connected-worker success.

Before applying the migration, root captured47 application-table hashes,16 evaluation rows and230 existing function definitions/ACLs. Exactly one queued/processing evaluation lacked disclosures. Runtime is quiesced for exact before/after comparison; principal identities will not be reset.

### Additional J08 connected results

Nine independent cases passed against the preview-P2 bound build: J08, J08a, J08b, J08c, J08e, J08d, J08-admission-paid, J08-admission-free and J08f. Their individual logs and retained failures are indexed in the private task-4 browser report. Original paid/free requests recover without replacement; complete proof loss stays unrecoverable; unpaid cancellation remains terminal; actual paid/free committed admission with a lost reply uses a read-only same-ticket recheck; a fresh Admitted result survives a failed totals refresh. Exact ticket identities, sibling states and Used timestamps are checked.

J08b first timed out on a disabled status button after the app had automatically confirmed the original RSVP. The retained body and source mount effect establish that harness mismatch; resume1 accepted the real existing confirmation and fenced registration creation. J08g remains a separate failed payment-contract branch. The overall32-case matrix currently has19 passes,2 paused failed branches and11 unexecuted cases; it is not complete.

### Moderation forward migration — applied preservation and SQL proof

`moderation-forward-migrate-execution.json` records the guarded command, actual execution times and exit0. The122-entry task ledger is the original121 exact entries followed by `20260918010100_guard_incomplete_moderation_jobs.sql`, SHA-256 `992a0de9840f90cf79d887215ab2d966b7857099698842fb4a17cea35c345f6e`. Its append authorization is [the approved forward manifest](spec14-approved-forward-migrations.json).

Before/after preservation passed:45 of47 application-table hashes are identical. The sole affected evaluation was superseded with `INCOMPLETE_MODERATION_INPUT`; its identity, revision, hash, attempt1 and existing start time stayed unchanged, as did all15 other evaluations. The only other table difference is the existing runner’s development-policy revalidation timestamp; all policy values remain equal. Exactly one existing function definition changed, two were added, all230 existing function ACLs stayed equal, and automatic jobs remain inactive. Event, financial, admission, report/review and publication-history tables did not change. Private proof: `moderation-preservation-result.json`.

The new enqueue suite passes4/4 and contract suite32/32, both rollback-only. Initial contract attempts retained two harness errors: pgTAP comparison of the catalog text array needed explicit C collation (after6 passes); whitespace-free `values(...)` was interpreted by installed pgTAP as a prepared-statement name (after22 passes). The corrected assertions still require the exact definer/search-path contract and original expected values. These failures did not require a production change. The independent concurrency-test guard finding also closed before execution; its preconditions conservatively include outside leases that could cross an expiry cutoff. The subsequent four-case concurrency retry passed; the original timeout and retry evidence remain recorded below.

### Regression introduced by the first moderation migration — corrected with a separately approved forward

The broader inherited run passed5 of7 suites. `moderation_published_edits.test.sql` passed72/73: assertion49 exposed a real regression introduced by the new migration. Root and the implementation reviewer compared against literal010550 and missed010575’s later dynamic `pg_get_functiondef` rewrite from `starts_at > v_now` to `ends_at > v_now`. The copied body caused a valid active-event end-time edit to lose its prior clear status. The earlier production-review closure is retracted; the original pre-migration suite had passed.

No principal browser/worker/event write ran after application; this failed fixture transaction rolled back. The preservation results still establish unchanged stored events/history, but do not establish preserved function behavior. The exact erroneous function hash was `b32f90c4bd9a3de4f28acf6f1af5ed78c36027a923fd58a7f6f1fe7506de9b1d`. Restoring only the inherited schedule check yields `e20ff4f33f4e1b008432ee6b855ce0e2380106f28e1559258d1ac1bc5481bc66`. Removing the new enqueue predicate from that corrected definition exactly reproduces the captured actual prior hash `67a624a83a362839ac71a296e35325ac2f5f20a60d9a2fe584ba9a35efc2d357`. Independent offline reconstruction and the inventory-wide dynamic rewrite audit agree.

The user separately approved the hash-fenced supplemental proposal (SHA-256 `b2f7f8bedb44689f4614c9b88866c0793553c21f3adda254f3a71532859101f5`). Its exact SQL bytes were applied as `20260918010200_restore_active_event_moderation_schedule.sql`, SHA-256 `827b264600d5e07adf0d2e8227ea0a284b56ed1f4c7bad09caf8af4aae89a087`. The ledger now has123 entries, with all previous122 files and ledger entries unchanged. No applied migration was rewritten, ledger repaired or database reset. The updated runtime subsequently passed J06c/J06d; the separate B2 Payments fixture remains pending.

The other broad failure, `public_eligibility_projections.test.sql`33/34, is a separate global-query fixture collision: a legitimate existing principal paid event is included in a query that previously ran against an otherwise empty database. Its unchanged map projection function is outside the repair scope; no map change or principal removal is proposed. The failed result remains retained.

The first concurrency run passed the reclaimed-lease/FIFO case, then timed out at the event advisory lock. PostgreSQL logs confirm the15-second statement timeout. Its unique fixtures were fully cleaned and outside evaluation rows matched exactly. The independently reviewed retry changes only bounded timing limits and private failure capture; the corrected retry passed all four cases, with exact outside-evaluation equality and verified fixture cleanup.

### Supplemental correction — local verification checkpoint

The guarded supplemental migration command exited0 (`moderation-supplement-migrate-execution.json`). Root and independent comparisons both pass: all47 application table counts and all16 complete evaluation rows are unchanged;46 table hashes match and the sole table difference is the existing policy revalidation timestamp. All232 function owners and ACLs match. The only definition change is the exact `b32f…` to `e20f…` restoration described above. All nine extension records match; automatic jobs remain inactive. Evidence: `moderation-supplement-preservation-result.json` and `moderation-supplement-independent-comparison.md`.

The subsequent targeted SQL command exited0 with all eight suites passing (`moderation-supplement-sql.log`): published edits (73 assertions), incomplete enqueue (4), incomplete input contract (32), schema, evaluations, staff actions, reviews/reports and publish eligibility. The contract fixture composes both approved migrations before its unchanged assertions. This result closes the demonstrated active-event SQL regression. The four subsequent concurrency cases also passed, exit0, with outside evaluation rows unchanged and unique fixtures fully cleaned (`moderation-supplement-concurrency-evidence.json`). Connected browser acceptance still requires the updated runtime.

### Post-correction type generation and static checks

The explicit local type-generation command exited0 and preserved the task instance/public-schema metadata binding. The exact generated-type diff adds only the six-argument service rejection RPC; no existing type changed. Generated type SHA-256: `bf6e300500f17cc238c5524e84e59c96dc4a7a8237f67177f6bd5ab983178a2c`. Full frontend/integration/E2E/scripts typecheck and full ESLint both exited0 (`moderation-supplement-typecheck-execution.json`, `moderation-supplement-lint-execution.json`).

A separate Deno `check --cached-only` invocation exited1 before checking files because this installed CLI supports that flag on `test`, not `check`. That failed invocation and log are retained. The supported offline `test --no-run --frozen` command subsequently exited0 for the function dependency graph (`moderation-supplement-functions-no-run-execution.json`); this is static checking, separate from the earlier347 executed function tests.

The J08 real-data runner received additional test-only guards after the full static checkpoint: strict retained dataset/registration identity, a conservative global-worker preflight that includes leases before they expire and rejects attempt-limit processing rows, and exact outside-evaluation comparison. Root inspected the source and independently reran the combined B2/J08 guard tests:14/14 passed, exit0 (`moderation-j08-guard-independent-execution.json`); the author also ran focused ESLint and syntax checks. Earlier weaker-guard and RED evidence remains retained. These controls do not claim an executed pagination/full-capacity browser result.

### Post-correction build and restart

The guarded build, start and running-identity verification each exited0. This candidate source has1,100 present files and4 inherited intentional deletions, canonical source SHA-256 `91897690be5cb05b81f6e29e2f73e7ace87918b42160d4c829c047e50cf54e3e`. Its149 built files retain assets SHA-256 `738da489f40f60016a41c8ec28f4a6b929a81dd225f2a25eefb453ee711e6a17`; HTTP reads independently matched every file (`moderation-supplement-served-asset-proof.json`). The updated worker loaded through the guarded restart and data was preserved. The original J06c revision5 now resumes under exclusive test ownership.

This is an interim build identity. Subsequent narrative/evidence updates and remaining browser work still require the final catalog, review, bound build and actual clean export. The previous build/source manifests remain archived separately; earlier browser evidence retains its original bindings.

### Retained frontend configuration failure

A fresh `pnpm test` invocation omitted the required synthetic public environment values and exited1:29 files failed to load or complete,150 files passed, and2 of1,250 executed assertions failed with2 reported errors. Root traced these import-time failures to missing configuration; the invocation is retained as harness evidence, not a new application regression. A guarded attempt to interrupt that exact owned test process found that it had already exited, so no signal or process mutation occurred. The full suite was rerun with the previously verified explicit synthetic Supabase/Mapbox/Stripe public values:179 files and1,532 assertions passed, but15 known unhandled Auth coordination errors caused exit1 (`post-moderation-full-frontend-configured-execution.json`). This command still fails; no error was suppressed. No provider credentials or hosted environment is used.


## Latest connected verification checkpoint

J06c passed after the repaired moderation worker evaluated the original revision5 and a bounded two-call email-worker drain processed five existing eligible intents, including the exact original recovery request. The readback created zero new recovery requests and retained both original collections and the original three tickets. The earlier discarded worker response remains unknown. J06d passed stale notice review rejection, suppression of the superseded unsent notice, and preservation of an accepted notice across a later publication. Original failed attempts remain retained in the private evidence index.

The additional J08 real-data runner passed both checks on its third resume: 25 admissions remained visible during the one-shot next-page failure, Retry recovered all30, and the capacity1 public RSVP route showed full. The two retained events were created/published through real UI; three10-admission registrations and one1-admission registration produced31 real tickets. Post-fixture records and the principal scenario were preserved exactly. The report recorded zero business-writer attempts, blocked requests and page errors in its post-fixture interval; however, its imported guard recognized gateway55647 and missed actual app3040 requests. The zero writer counter therefore does not establish pre-transport fence coverage. Authorized UI fixture creation is listed separately. An explicit retained-data `--read-only` rerun is pending under the corrected guard. All three prior partial attempts remain: omitted minimum age, a radio locator for its select, and a read before the final details-navigation wait. The last timing explanation is inferred because the failing row was not captured. No replacement event or registration cleared a failed attempt. The passing report ran from 06:50:26 through07:00:12UTC on2026-09-15 and is bound by SHA-256 `50beea01690d5c06abbf7950fb24d1b33e19df60dd9f6c163bae19e8715e1d0b`.

These checks used source identity `91897690be5cb05b81f6e29e2f73e7ace87918b42160d4c829c047e50cf54e3e` and assets `738da489f40f60016a41c8ec28f4a6b929a81dd225f2a25eefb453ee711e6a17`. Subsequent test and documentation edits require the final source/build binding; this is an interim execution identity.


### Browser guard correction checkpoint

Independent review found three browser writer predicates that checked gateway55647 but missed app3040: B2 Payments, the shared J08 read-recovery helper, and the separate J06 legacy-history helper. J08 real-data imported the shared helper. All three predicates now cover both exact local origins; the29 focused tests, eight-file lint and four runner syntax checks pass independently. B2 also targets the actual same-origin status POST and measures each failure/retry/exit interval separately. J08 real-data binds both directly imported guard files and has an explicit read-only mode that refuses missing/partial retained data, disables provider actions and installs the fence before page creation.

The earlier reports remain unchanged. Their actual UI outcomes and recorded database comparisons are retained; their zero writer counts are qualified and must not be presented as complete same-origin protection. The corrected B2 retry completed four checks, then reproduced missing error UI and11 browser errors after exactly one external script failure. All selected event/tier/ticket facts and the principal scenario were preserved. This is a demonstrated frontend load-handling gap; a bounded public-API readiness repair is being implemented under the existing Gate B UI authorization, with no dependency or payment-contract change. Review: `browser-guard-independent-review.md`, SHA-256 `5514c6201e30036d02af2439c4a744cc2d24271e0092ca7ce3772092d9265e58`.


### Corrected-fence runtime results

`j08-read-recovery-1789457955214/report.json` passed all three checks from07:39:05 through07:40:09UTC on2026-09-15: original free email-status drop/retry, cached selected-ticket detail failure/retry, and unavailable public-ticketing read/retry. The corrected guard recognized app3040 and gateway55647, with zero writer attempts, outbound blocks and page errors. Its report SHA-256 is `5be9fccf053f09091fb6a021aa0b2c01df792169a64c1fa438c13bf614b525fc`; runner SHA-256 is `af278934ac3892dd5d2efeaebc7cd09327e093c487efaee8d4d515532da8ba4f`. The old unfenced report remains qualified separately.

`b2-payments-1789457579460/report.json` (SHA-256 `354807cf9e13ebdbc34989b4b13e6c51b39d50eef419d9f4c57bced675f5ed71`) passes same-draft prompt Back/native history/reconfirmation, a measured status POST failure and distinct retry plus same-draft return, invalid-ID/wrong-owner rejection, and the published principal event's two active tiers. The final external-script stage fails: one actual connect.js request was dropped, the embedded error heading did not appear, and11 page errors were recorded. Zero domain writer attempts were seen by the corrected guard; source and scenario comparisons pass. Browser/process contexts closed. The failed script branch does not invalidate those four completed checks, and those checks do not close the failed branch.


The explicit J08 real-data read-only rerun also passed: `j08-real-data-1789458020058/report.json`, SHA-256 `ceb1da2ee9ad1a3a52744f3674564025ca23594d67a456dbdac59e82f2a69ebc`,07:40:20→07:46:52UTC. It reused the exact two published events, four registrations and31 tickets; all fixture/provider/worker setup was disabled. Pagination retained25 rows through one failed read and recovered30; capacity1 remained full. The corrected guards ran before browser work and recorded zero writer attempts, outbound blocks and page errors; dataset/scenario preservation passed. Both directly imported guard files were bound alongside the runner. This closes the read-only fence gap for those two checks without creating any replacement data.


The corrected legacy control also passed (`j06-legacy-history-1789458862720/report.json`, SHA-256 `6add8d5a0f26d942ba8355a2dcd5c1dc106e2ba9f96eeb425b68cbd25228a7d9`,07:54:22→07:55:27UTC). Its explicit allowlist adds only the current user's existing staff-role read; unknown RPCs still reject. The real page shows the missing prior-history messages and current saved/public facts, with zero browser writers, blocked requests or page errors. Cleanup passed and scenario/provider bytes stayed unchanged; the ordinary snapshot sequence advanced163→164 without rewind. The immediately preceding guard mismatch and its162→163 advance remain recorded.

After the frontend-only Payments readiness change, Root reran the full configured frontend suite:181 files/1,544 assertions PASS,15 known unhandled Auth errors, process exit1. Full typecheck and lint both exited0. This is static/unit evidence for the new candidate; the actual external-script recovery rerun remains pending its independent review and bound rebuild.

## Payments readiness — pre-freeze browser checkpoint

The six-file public Connect-script readiness repair passed independent source review,18 focused tests and four additional loader-lifecycle probes (`b2-readiness-independent-review.md`, SHA-256 `9ffc3396a66f9c0b572027a8a134e98b0e59be595c3bf77cdafce979f03c123b`). Root rebuilt and restarted the local app; all149 actual HTTP assets matched source `9f7c92fc2a9e4e8d8a49a572dc5f6eca6eddd73e8ab04ad7f0555eaef0510426`, assets `e26eb4b2c24eb38c86416c8fa6a439b10fdc7127b6256ea09397d62ca21e9de3`.

`b2-payments-1789459730279/report.json` (SHA-256 `e50df989be7e814eef38f95d07db7eec8c66cb4c2d7336957514e972283596f0`) passed all five behavior checks. Exactly one external script request was dropped; inner Try again requested the second script and recovered the simulated provider content. Opening created one Account Session; retry and exit created none. Exit produced one fresh status request and returned focus. Original event, tiers, tickets and principal scenario were preserved, with zero domain writers or blocked requests. The process still exited1 because the local provider element lacked its section-open callback setter and emitted one page error. This is retained as a partial run, not a clean PASS. The simulator's narrow callback-contract correction and final bound rerun are tracked in the detached evidence; no SDK dependency, application authority or provider account change is authorized by that test correction.

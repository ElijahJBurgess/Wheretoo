# Spec 12 implementation and verification

The approved shared error, empty and loading scope is implemented in the isolated Spec 12 worktree. Local verification passes. The three map runtime states remain explicitly deferred. No commit, merge, push, deployment, provider operation or shared database mutation was performed.

1. **Worktree and branch.** `/Users/exoh/Desktop/WhereTo -  Repository/.worktrees/spec12-shared-states`, branch `codex/spec12-shared-states`. The branch remains uncommitted at `94c546bd0961f501584f8a3437298b2331cafd5c`; that commit alone does not represent the delivered implementation.

2. **Accepted frozen baseline.** Source: `.worktrees/spec10-spec11-integration`, branch `codex/spec10-spec11-integration`, manifest `.superpowers/spec10-spec11/frozen-baseline.json`. All 904 mapped files were copied and hash-verified before editing; all 904 source files still match after implementation. File-map SHA-256: `08b7c552752c0a05a935e6e839806e34952d3ecb4f257b4f1108204c0e5864f2` (UTF-8 Python `json.dumps(files, sort_keys=True)`, default separators). Manifest-file SHA-256: `3bc5074186070e0e7d05c0411e56a3c55bc99530bdd6d35d1a0f69dadaaed66f`. Review changes against this manifest, because ordinary Git status also contains inherited uncommitted Specs 10/11 files.

3. **Files and schema.** The implementation changes 58 baseline files and adds 17 implementation/test/configuration files, plus this report and its complete file inventory. The sibling `spec12-changed-files.md` lists every path. Shared components, startup/router handling, real buyer and organizer adapters, styles, development preview and tests comprise the changes. **Zero migrations, schema/RLS changes, dependency changes or provider writer changes.** Protected files, including all Supabase files, Auth implementation and operation recovery modules, were hash-checked against the baseline. The cancellation UI eligibility guard changed; its writer and recovery operation identity did not.

4. **AsyncState.** Existing callers remain supported. The component presents loading, empty, error, not-found, denied, unavailable and offline states, with parent-supplied primary/secondary actions and heading levels. Four decorative skeletons cover event cards, metrics, order rows and detail fields. Skeletons contain no invented dollar values, counts, tickets, statuses or QR patterns. Shared presentation contains no query client, Auth/route inference, mutation, retry effect or reconnect effect. Busy/live announcements, decorative icon semantics and reduced-motion styles are covered.

5. **Startup and routing.** Unmatched routes, render exceptions and lazy import failures render sanitized shared fallbacks. Pending lazy imports now render an eager “Loading page” fallback instead of a blank body. Route-specific fallbacks remain respected, and mounted parent shells survive child failures where routing permits. Initial application imports use a catch boundary; initial React/provider rendering has a top-level error boundary. A final shell-free startup fallback needs no application provider. Copy never prints exceptions, stacks or private parameters. Navigation targets the existing sign-in route; no generic Reload recovery or invented consumer homepage was added.

6. **Connectivity.** A passive browser hint observes online/offline events. It makes no provider availability claim. ReadState turns an initial loading state into “Waiting for a connection” when the browser hints offline or the owning query is paused. React Query retains ownership of canonical reconnect reads. A real-provider/read-adapter test proves one paused-read resume and one reconnect read, without a duplicate shared effect or automatic write.

7. **Buyer adaptations.** Public event reads, checkout initialization, order confirmation reads, paid ticket collections, RSVP reads/collections, ticket email access, refund details and event status access use shared presentation while retaining domain recovery states. Public explicit absence can render not-found; malformed/null response envelopes are rejected. No missing metric becomes zero. Existing support/legal configuration remains authoritative; absent support configuration does not create a contact destination.

8. **Organizer adaptations.** My Events distinguishes an authorized complete zero (“No events yet”) from filter no-match (“No matching events”), with the appropriate create or clear-filter action. Orders distinguishes zero from search/status no-match and retains previously loaded pages after a later-page failure. Guest search remains idle while blank, distinguishes a successful no-match from an unavailable search, and retains safe loaded pages during pagination failure. Free registration lookup follows the same rules without paid-order semantics. Dashboard/detail, editor/publishing, ticket-tier, moderation and event-change reads receive shared loading/unavailable treatment. Unsupported free check-in remains unavailable. Cancellation controls require fresh verified eligibility; a failed newer read invalidates older eligibility while preserving an existing unknown operation and its explicit reconciliation UI.

9. **Private classification.** Ambiguous private failures remain neutral Unavailable without extra existence/ownership probes. Explicit denied presentation is limited to a successful authoritative own-role result; missing role data remains unavailable. Session authority and validated sign-in returns remain Spec 11 owned. Read failure does not imply anonymous or expired session. Existing local grant expiration/revocation classifications remain intact.

10. **Malformed checkout.** Invalid UUIDs and missing identifiers, including `/events/checkout` and `/events//checkout`, reach “Checkout unavailable” before checkout initialization. Unit regressions cover malformed links with cancellation parameters and retained recovery storage. Browser interception observes zero backend requests for invalid/missing identifiers. The path creates/replaces no checkout attempt, clears no bearer, and starts no payment operation. Existing verified navigation remains available.

11. **Identity/resource-switch proof.** The full suite exercises session/private-cache boundaries and pending resource changes. New paid collection A→B→A coverage proves old QR content disappears and late B results cannot replace the current A collection; only one QR is mounted. New RSVP A→B→A coverage exposed and fixed a reused bearer lifetime by remounting the private collection at its bearer boundary. Cancellation presentation is keyed by owner/event, and pending recovery remains mounted across same-resource refresh failure. Existing owner/resource switch, late response, grant and focused-ticket tests also pass.

12. **Transaction regression proof.** The complete frontend suite includes Specs 08 payment attempt/cancel recovery, 09 refund unknown/completed recovery, 10 cancellation reconciliation, 05 scanner/manual admission uncertainty, 06 RSVP timeout/capacity identity, 07 delivery/send recovery, and 11 session/private-cache/profile/Connect behavior. New cancellation tests prove cached published data cannot authorize a fresh cancellation; unknown recovery survives refresh failure and close/reopen; explicit reconciliation is a read; a later failed freshness read disables older eligibility again; the original submit count remains one. Shared read retries do not invoke transactional mutations. All 327 function tests pass. This is local regression evidence, not a new provider end-to-end transaction certification.

13. **Final checks.** `pnpm test --maxWorkers=4`: **147 files, 1,279 tests passed**. Worker limiting runs the complete `pnpm test` suite. `pnpm exec playwright test --config playwright.spec12.config.ts`: **8 passed**. `pnpm typecheck`, `pnpm lint`, `pnpm build`, and `git diff --check`: passed. `pnpm test:functions` (`deno task test`): **327 passed, 0 failed**. `pnpm typecheck:functions` (`deno task check`): passed. Production output excludes the shared-state development gallery. The final frontend run prints a jsdom navigation diagnostic but has no failed test. The initial cold baseline had one CLI timing timeout; the isolated baseline rerun and complete final suite both pass. Final review found no outstanding actionable issues.

14. **Browser, responsive and accessibility results.** Headless Chromium checked the gallery at 320, 390, 768 and 1440 CSS pixels, with no horizontal overflow. Screenshots were inspected for wrapping, contrast, spacing and skeleton composition, including scrolled gallery states and real buyer loading/error/malformed-checkout states. Browser routes use real application adapters with controlled synthetic transport. Slow loading → error → explicit safe retry → success, authoritative public absence, offline hints, sanitized unknown routes and invalid checkout were exercised. Keyboard focus, heading semantics, alert/status announcements, busy state, hidden decorative skeletons and reduced-motion suppression pass automated assertions. Search-clear focus and async transitions also have component coverage. This is targeted accessibility coverage, not a full screen-reader or WCAG audit.

15. **DEFERRED — MAP OWNER.** “No Events on Map,” “Location Permission Denied,” and “Map Failed” exist only as clearly labeled synthetic development fixtures. None counts as runtime acceptance. Map runtime files were not changed.

16. **Proofs intentionally skipped.** No Stripe, Supabase, Resend, email, payout, shared environment or deployed application operations were performed. Physical iOS/Android devices, actual camera admission, physical screen-reader interaction and real network/provider outages were not tested. No SQL suite was rerun: schema, RLS, functions and database contracts were unchanged, and the new adapters use existing reads. Existing SQL evidence remains baseline evidence, not fresh Spec 12 evidence. The local browser config uses synthetic localhost credentials/transport and does not require a provider account.

17. **Remaining blockers and setup.** No remaining blocker was found for the approved non-map Spec 12 scope. Map runtime acceptance and the skipped external/device proofs remain outside this completion claim. No production manual setup is required. To repeat local browser coverage, run the Spec 12 Playwright command from this worktree; its configuration starts the isolated local Vite server with synthetic environment values. Keep this uncommitted worktree and the frozen source intact for the next integration step.

Evidence is retained under `.superpowers/spec12/`: the accepted manifest, approved request, isolation verification, complete incremental diff against frozen source, changed/added file inventories, RED/GREEN regression logs and final check logs. Browser screenshots are under `test-results/spec12-shared-states/`. These generated local artifacts are not committed.

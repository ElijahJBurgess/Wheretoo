STRIPE_RESTRICTED_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_THIN_WEBHOOK_SECRET=
APP_BASE_URL=
MODERATION_WORKER_TOKEN=
REPORT_FINGERPRINT_SECRET=
CONTEXTUAL_MODERATION_ENDPOINT=
CONTEXTUAL_MODERATION_BEARER_TOKEN=
# Generate 32 random bytes, then convert the output to canonical unpadded base64url.
# Example: openssl rand -base64 32 | tr '+/' '-_' | tr -d '=\n'
TICKET_CREDENTIAL_SECRET=

# Spec 07 remains inactive until separately approved sender/support and limit setup.
TICKET_EMAIL_WORKER_ENABLED=false
TICKET_EMAIL_PUBLIC_ENABLED=false
TICKET_EMAIL_WORKER_SECRET=
TICKET_EMAIL_FROM=
TICKET_EMAIL_SUPPORT_EMAIL=
TICKET_EMAIL_PAYLOAD_KEY_ID=
TICKET_EMAIL_PAYLOAD_KEYS_JSON=
TICKET_EMAIL_RATE_SECRET=
# Only a header proven to be overwritten by the trusted ingress; no forwarded fallback.
TICKET_EMAIL_TRUSTED_IP_HEADER=
RESEND_WEBHOOK_SECRET=
